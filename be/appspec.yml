# appspec.yml

version: 0.0
os: linux

files:
  - source:  /
    destination: /home/ubuntu/shareFit
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: be/scripts/stop.sh
      timeout: 60
      runas: ubuntu
  ApplicationStart:
    - location: be/scripts/start.sh
      timeout: 60
      runas: ubuntu